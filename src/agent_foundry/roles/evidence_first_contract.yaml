name: evidence_first_contract
description: "Enforces evidence-first policy \u2014 no recommendation without evidence"
version: 1.0.0
implementation:
  module: agent_foundry.roles.evidence_first
  class_name: EvidenceFirstContractRole
tags:
- validation
- eval_gate
- evidence
- contract
quality_controls:
  timeout_seconds: 10
  max_retries: 0
