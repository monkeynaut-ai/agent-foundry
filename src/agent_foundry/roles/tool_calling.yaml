name: tool_calling
description: Executes tool calls with argument validation and result capture
version: 1.0.0
implementation:
  module: agent_foundry.roles.tool_calling
  class_name: ToolCallingRole
tags:
- tools
- execution
quality_controls:
  timeout_seconds: 60
  max_retries: 1
