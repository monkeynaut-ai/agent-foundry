name: schema_validator
description: Validates output against a JSON schema and blocks on failure
version: 1.0.0
implementation:
  module: agent_foundry.roles.schema_validator
  class_name: SchemaValidatorRole
tags:
- validation
- eval_gate
- schema
quality_controls:
  timeout_seconds: 10
  max_retries: 0
