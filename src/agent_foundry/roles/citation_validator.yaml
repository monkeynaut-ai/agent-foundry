name: citation_validator
description: Validates that all cited evidence IDs exist in retrieved evidence
version: 1.0.0
implementation:
  module: agent_foundry.roles.citation_validator
  class_name: CitationValidatorRole
tags:
- validation
- eval_gate
- citation
quality_controls:
  timeout_seconds: 10
  max_retries: 0
