[project]
name = "agent-foundry"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "mark norman", email = "mark@marknorman.com"},
]
dependencies = ["pydantic>=2.12.5", "pytest>=9.0.2", "pyyaml>=6.0.3"]
requires-python = "==3.14.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
